# Benchmarks

Harnesses for latency, tool correctness, and regression tracking.

```bash
uv run python benchmarks/run_latency.py --mock
uv run slm bench latency --mock   # alias via CLI when wired
```
